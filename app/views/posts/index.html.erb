<div class="container">
  <header>
    <h1>一瞬で忘れるつぶやき投稿アプリ</h1>
    <h1>DACHO</h1>
  </header>

  <div class="main">
    <h1>投稿一覧</h1>
    <p class="content"><%= notice %></p>

    <table class="content">
      <tr>
        <th>投稿内容</th>
      </tr>

      <% @posts.each do |post| %>
        <tr>
          <td><%= post.content %></td>
          <td><%= link_to '内容を確認する', post_path(post.id) %></td>
          <td><%= link_to "編集する", edit_post_path(post.id), data: { confirm: '本当に編集していいですか？' } %>
          <td><%= link_to '削除する', post_path(post.id), method: :delete ,data: { confirm: '本当に削除していいですか？' } %>
        </tr>
      <% end %>
    </table>

    <div class="new_button">
      <%= link_to '新しく投稿する', new_post_path, class: "new_link" %>
    </div>

  </div>

        <footer class="footer">
          <p>一瞬で忘れる機能は実装されておりません</p>
        </footer>

</div>